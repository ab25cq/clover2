class CommandTest {
    def main():static {
#        Command.ls("-al").grep("clcl").less();
#        Command.pwd().less();

#        Clover.jobs.items(0).forground();

        Command.pwd().toString().print();
    }
}
