
a:Array<Integer> = {1, 2}.toArray;

/*
a.each(lambda(item:Integer)
{
    System.println(item.toString);
});
*/


/*
a:Array<Integer> = {1,2,3}.toArray;

a.each(lambda(item:Integer) {
    System.println(item.toString);
}
*/
