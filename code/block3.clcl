class LabmdaTest {
    value: lambda(int, int):int;

    def initialize() {
        self.value = lambda(a:int, b:int):int {
            return a + b;
        }
    }

    def run(a:int, b:int):int {
        value_ := self.value;
        return value_(a, b);
    }
}
